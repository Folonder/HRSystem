@model Presentation.Models.Department.DepartmentCloseModel

@{
    ViewData["Title"] = "Закрыть подразделение";
}

<h1>Закрыть подраздление</h1>

<h3>Вы уверены, что хотите закрыть подразделение и все его дочерние подразделения?</h3>

<form asp-action="Close" method="post">
    <input type="hidden" asp-for="DepartmentId" />
    <div class="form-group">
        <label asp-for="CloseDate">Дата закрытия</label>
        <input asp-for="CloseDate" class="form-control" type="date" />
        <span asp-validation-for="CloseDate" class="text-danger"></span>
    </div>
    <p></p>
    <div class="form-group">
        <input type="submit" value="Закрыть" class="btn btn-danger" />
        <a asp-action="Index" class="btn btn-secondary">Отменить</a>
    </div>
</form>