@model Domain.Entities.Department

<h2>Удалить департамент</h2>

<h3>Вы уверены, что хотите удалить департамент и всю его историю?</h3>
<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Название
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class="col-sm-2">
            Родительское подразделение
        </dt>
        <dd class="col-sm-10">
            @(Model.ParentDepartment != null ? Model.ParentDepartment.Name : "Нет родительского подразделения")
        </dd>
    </dl>
    <div class="form-group">
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Удалить" class="btn btn-danger" />
        <a asp-action="Index" class="btn btn-secondary">Назад к списку</a>
    </form>
    </div>
</div>